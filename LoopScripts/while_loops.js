// Define a script using a while loop to print "I love loops" 7 times
let count = 0; // Initialize a counter

while (count < 7) {
  console.log("I love loops"); // Print the phrase
  count++; // Increment the counter after each iteration
}
